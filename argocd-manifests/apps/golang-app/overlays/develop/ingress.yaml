apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: golang-app-ingress
  annotations:
    cert-manager.io/cluster-issuer: "local-ca-issuer"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - 'dev-golang.argocd-learn.local'
        - '*.argocd-learn.local'
      secretName: local-tls
  rules:
  - host: dev-golang.argocd-learn.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: dev-golang-app
            port:
              number: 80