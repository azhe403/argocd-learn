apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: golang-app-ingress
  namespace: argocd-learn
  annotations:
    cert-manager.io/issuer: "local-ca-issuer"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - 'golang.argocd-learn.local'
        - '*.argocd-learn.local'
      secretName: local-tls
  rules:
  - host: golang.argocd-learn.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: prod-golang-app
            port:
              number: 80