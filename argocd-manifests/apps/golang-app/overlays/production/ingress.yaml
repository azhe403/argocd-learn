apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: golang-app-ingress
  annotations:
    cert-manager.io/cluster-issuer: "argocd-learn-ca-issuer"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - "golang-app.argocd-learn.azhe.my.id"
      secretName: production-golang-tls
  rules:
    - host: golang-app.argocd-learn.azhe.my.id
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: production-golang-app
                port:
                  number: 80
