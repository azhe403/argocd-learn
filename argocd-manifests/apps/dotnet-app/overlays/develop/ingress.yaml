apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dotnet-app-ingress
  annotations:
    cert-manager.io/cluster-issuer: "argocd-learn-ca-issuer"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - "dev-dotnet-app.argocd-learn.azhe.my.id"
      secretName: develop-dotnet-tls
  rules:
    - host: dev-dotnet-app.argocd-learn.azhe.my.id
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: develop-dotnet-app
                port:
                  number: 80
